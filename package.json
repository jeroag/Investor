{
  "name": "cryptoplan-ia",
  "version": "2.0.0",
  "description": "CryptoPlan IA — App de trading con IA (Supabase + módulos)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/calculations.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.43.4",
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "ws": "^8.17.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
